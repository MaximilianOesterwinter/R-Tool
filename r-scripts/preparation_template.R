library(readr)
library(dplyr)
library(readxl)

# Read your dataset with the correct command.
df <- 

saveRDS(df, 
        #Replace the example with the correct absolute output-path.
        #It is important, that you save the file in .../RTool/data/prepared/!!
        "/home/YourName/workspace/RTool/data/prepared/example.rds"
               ##################                    ########
        )
